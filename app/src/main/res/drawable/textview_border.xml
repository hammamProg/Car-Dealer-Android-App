<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/navigation_bar_item_BG_color" /> <!-- Background color -->
    <corners android:radius="10dp" /> <!-- Corner radius, adjust as needed -->
    <stroke
        android:width="2dp"
        android:color="@color/navigation_bar_item_border_color" /> <!-- Border color (white in this case) -->
</shape>
